
const MAIN = "tdmanager";
const PRODUCT = `${MAIN}.PRODUCT`;
const PROVIDER = `${MAIN}.PROVIDER`;
const SHELF = `${MAIN}.SHELF`;
const UNIT = `${MAIN}.PRODUCT_UNIT`;
const BATCH = `${MAIN}.BATCH`;
const STOCK = `${MAIN}.STOCK`;
const USER_ROLE = `${MAIN}.USER_ROLE`;
const USER = `${MAIN}.USER`;
const RESTOCK_PROGRESS = `${MAIN}.RESTOCK_PROGRESS`;

const RESTOCK_PROCEDURE = `${MAIN}.update_restock_progress`;
module.exports = {
    
    PRODUCT,
    PROVIDER,
    SHELF,
    UNIT,
    BATCH,
    STOCK,
    USER_ROLE,
    USER,
    RESTOCK_PROGRESS,
    RESTOCK_PROCEDURE,
}